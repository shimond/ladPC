import { OnlyHebrewDirective } from './only-hebrew.directive';

describe('OnlyHebrewDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyHebrewDirective();
    expect(directive).toBeTruthy();
  });
});
